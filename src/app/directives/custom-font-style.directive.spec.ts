import { CustomFontStyleDirective } from './custom-font-style.directive';

describe('CustomFontStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomFontStyleDirective();
    expect(directive).toBeTruthy();
  });
});
