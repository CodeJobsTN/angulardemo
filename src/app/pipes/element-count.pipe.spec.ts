import { ElementCountPipe } from './element-count.pipe';

describe('ElementCountPipe', () => {
  it('create an instance', () => {
    const pipe = new ElementCountPipe();
    expect(pipe).toBeTruthy();
  });
});
